<ul class="photo-palette" webc:if="palette?.length" aria-label="Photo colour palette">
    <li webc:for="(index, hex) in palette">
        <span :style="`background-color: ${hex};`" :title="hex"></span>
        <span class="u-visually-hidden" @text="hex"></span>
    </li>
</ul>
